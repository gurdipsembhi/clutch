# Get a key from https://aistudio.google.com/apikey
GOOGLE_API_KEY=your_gemini_api_key_here

# Optional overrides
GEMINI_MODEL=gemini-2.5-flash
TIMEZONE=Asia/Kolkata
# Comma-separated origins, or * for any (no cookies are used)
# ALLOWED_ORIGINS=["*"]
